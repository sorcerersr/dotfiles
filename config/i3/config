# This file has been auto-generated by i3-config-wizard(1).
#
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod4
set $alt Mod1


set $black       #212121
set $darkblack   #000000
set $red         #fb4934
set $darkred     #900800
set $green       #b8bb26
set $darkgreen   #98971a
set $yellow      #fabd2f
set $darkyellow  #d79921
set $blue        #83a598
set $darkblue    #1095f6
set $magenta     #d3869b
set $darkmagenta #b16286
set $cyan        #8ec07c
set $darkcyan    #689d6a
set $white       #eefffe
set $darkwhite   #a89984
set $transparent #00000055

# activate compositing
exec --no-startup-id picom -b --no-fading-openclose

# dpi/resolution setzen
# use when needed
# exec --no-startup-id xrandr --dpi 155

# default
# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:DejaVu Sans Mono 12

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# keybind: start a terminal
bindsym $mod+Return exec kitty

# kill focused window
bindsym $mod+Shift+q kill;

# start rofi
bindsym $mod+d exec rofidrun
bindsym $mod+Shift+d exec rofirun

# change focus
bindsym $mod+j focus left
bindsym $mod+k focus down
bindsym $mod+l focus up
bindsym $mod+odiaeresis focus right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move left
bindsym $mod+Shift+k move down
bindsym $mod+Shift+l move up
bindsym $mod+Shift+odiaeresis move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+h split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# define workspace destinations
# case dual monitor internal+hdmi workspaces 1-10 on hdmi
# and 11-20 (F1-F10) on internal
# in case of only internal all workspaces on internal but if internal
# not available use hdmi

workspace "1" output HDMI-A-0 eDP
workspace "2" output HDMI-A-0 eDP
workspace "3" output HDMI-A-0 eDP
workspace "4" output HDMI-A-0 eDP
workspace "5" output HDMI-A-0 eDP
workspace "6" output HDMI-A-0 eDP
workspace "7" output HDMI-A-0 eDP
workspace "8" output HDMI-A-0 eDP
workspace "9" output HDMI-A-0 eDP
workspace "10" output HDMI-A-0 eDP

workspace "F1" output eDP HDMI-A-0
workspace "F2" output eDP HDMI-A-0
workspace "F3" output eDP HDMI-A-0
workspace "F4" output eDP HDMI-A-0
workspace "F5" output eDP HDMI-A-0
workspace "F6" output eDP HDMI-A-0
workspace "F7" output eDP HDMI-A-0
workspace "F8" output eDP HDMI-A-0
workspace "F9" output eDP HDMI-A-0
workspace "F10" output eDP HDMI-A-0

# switch to workspace
bindsym $mod+1 workspace "1"
bindsym $mod+2 workspace "2"
bindsym $mod+3 workspace "3"
bindsym $mod+4 workspace "4"
bindsym $mod+5 workspace "5"
bindsym $mod+6 workspace "6"
bindsym $mod+7 workspace "7"
bindsym $mod+8 workspace "8"
bindsym $mod+9 workspace "9"
bindsym $mod+0 workspace "10"
bindsym $mod+F1 workspace "F1"
bindsym $mod+F2 workspace "F2"
bindsym $mod+F3 workspace "F3"
bindsym $mod+F4 workspace "F4"
bindsym $mod+F5 workspace "F5"
bindsym $mod+F6 workspace "F6"
bindsym $mod+F7 workspace "F7"
bindsym $mod+F8 workspace "F8"
bindsym $mod+F9 workspace "F9"
bindsym $mod+F10 workspace "F10"

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace "1"
bindsym $mod+Shift+2 move container to workspace "2"
bindsym $mod+Shift+3 move container to workspace "3"
bindsym $mod+Shift+4 move container to workspace "4"
bindsym $mod+Shift+5 move container to workspace "5"
bindsym $mod+Shift+6 move container to workspace "6"
bindsym $mod+Shift+7 move container to workspace "7"
bindsym $mod+Shift+8 move container to workspace "8"
bindsym $mod+Shift+9 move container to workspace "9"
bindsym $mod+Shift+0 move container to workspace "10"
bindsym $mod+Shift+F1 move container to workspace "F1"
bindsym $mod+Shift+F2 move container to workspace "F2"
bindsym $mod+Shift+F3 move container to workspace "F3"
bindsym $mod+Shift+F4 move container to workspace "F4"
bindsym $mod+Shift+F5 move container to workspace "F5"
bindsym $mod+Shift+F6 move container to workspace "F6"
bindsym $mod+Shift+F7 move container to workspace "F7"
bindsym $mod+Shift+F8 move container to workspace "F8"
bindsym $mod+Shift+F9 move container to workspace "F9"
bindsym $mod+Shift+F10 move container to workspace "F10"

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart


# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym j resize shrink width 10 px or 10 ppt
        bindsym k resize grow height 10 px or 10 ppt
        bindsym l resize shrink height 10 px or 10 ppt
        bindsym odiaeresis resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

bindsym $mod+r mode "resize"

# Workspace switches
bindsym $mod+x workspace next
bindsym $mod+y workspace prev
bindsym $mod+z workspace prev

bindsym Control+$alt+x workspace next
bindsym Control+$alt+y workspace prev
bindsym Control+$alt+z workspace prev

# multi monitor schieben
bindsym $mod+Shift+x move workspace to output right
bindsym $mod+Shift+y move workspace to output left
bindsym $mod+Shift+z move workspace to output left

# browsers
bindsym $mod+i exec firefox
bindsym $mod+Shift+i exec chromium


# file manager
bindsym $mod+p exec pcmanfm 

# Lockscreen
set $Locker i3lock -i /opt/wallpaper_arch/arch_lock_1080.png && sleep 1

bindsym $mod+Escape exec --no-startup-id $Locker

# power  menu
set $mode_system System (l) lock, (e) logout, (u) suspend, (r) reboot, (s) shutdown
mode "$mode_system" {
    bindsym l exec --no-startup-id $Locker, mode "default"
    bindsym e exec --no-startup-id i3-msg exit, mode "default"
    bindsym u exec --no-startup-id systemctl suspend, mode "default"
    bindsym r exec --no-startup-id systemctl reboot, mode "default"
    bindsym s exec --no-startup-id systemctl poweroff -i, mode "default"

    # back to normal: Enter or Escape
    bindsym Return mode "default"
    bindsym Escape mode "default"
}

# power menu keybindings
bindsym $mod+Shift+Pause mode "$mode_system"
bindsym XF86PowerOff mode "$mode_system"
bindsym $mod+Shift+e mode "$mode_system"

# start bar (i3blocks)
# bar {
#       colors {
#         statusline         $white
#         background         $transparent
#         separator          $blue

#         #                  BORDER       BACKGROUND   TEXT
#         focused_workspace  $transparent $transparent $darkblue
#         inactive_workspace $transparent $transparent $white
#         active_workspace   $transparent $transparent $white
#         urgent_workspace   $darkred     $darkred     $white
#         binding_mode       $transparent $transparent $blue
#     }

#     strip_workspace_numbers yes
#     font pango:DejaVu Sans Mono 10, FontAwesome 10
#     position top
#     separator_symbol " "
#     status_command i3blocks
#     tray_output eDP
# }

# start bar (polybar)
exec --no-startup-id runpolybar.sh

# window styles

#no window title but thicker border
for_window [class="^.*"] border pixel 2

#                       BORDER      BACKGROUND  TEXT   INDICATOR   CHILD_BORDER
client.focused          $darkblue   $darkblue   $black $darkblue   $darkblue
client.unfocused        $black      $black      $white $black      $black
client.focused_inactive $black      $black      $white $black      $blue
client.urgent           $darkred    $darkred    $black $darkred    $darkred
client.background       $black

focus_follows_mouse yes

gaps inner 5
gaps outer 0
smart_gaps on
#smart_borders on

bindsym XF86MonBrightnessUp exec "brightnessctl s +5%; notify-send 'Brightness up' && pkill -RTMIN+1 i3blocks"
bindsym XF86MonBrightnessDown exec "brightnessctl s 5%-; notify-send 'Brightness down' && pkill -RTMIN+1 i3blocks"

# launch udiskie - usb media automount
exec --no-startup-id udiskie

# nitrogen handles wallpapers
exec --no-startup-id nitrogen --restore

# network manager 
exec --no-startup-id nm-applet


# bluetooth
exec --no-startup-id blueberry-tray

# polkit to be asked for sudo password when an applications need root permissions, etc.
exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1

# Path to volume control, without trailing slash
set $volumepath ~/.local/bin/

# Command for the status line (used with -t, requires -u)
#   ie: i3blocks, i3status
set $statuscmd i3blocks

# Signal used to update the status line (used with -u, requires -t)
#   i3blocks uses SIGRTMIN+10 by default
#   i3status uses SIGUSR1 by default
set $statussig SIGUSR1

# Amount to increase/decrease volume as a percentage (used with -i, -d)
set $volumestep 5

# pulseaudio-utils settings when not using alsa-utils
#
# Symbolic name for sink (numeric index not supported) (used with -s $sinkname)
#   Recommended: comment out this setting and omit the -s option to use default sink
#   List sink names: pacmd list-sinks | awk -F "[<>]" '/^\s+name: <.*>/{print $2}'
set $sinkname alsa_output.pci-0000_00_1b.0.analog-stereo

# Using pulseaudio-utils (append "-s $sinkname" without quotes to override default sink)
bindsym XF86AudioRaiseVolume exec $volumepath/i3-volume -y -np -i $volumestep -t $statuscmd -u $statussig && pkill -RTMIN+1 i3blocks
bindsym XF86AudioLowerVolume exec $volumepath/i3-volume -y -np -d $volumestep -t $statuscmd -u $statussig && pkill -RTMIN+1 i3blocks
bindsym XF86AudioMute        exec $volumepath/i3-volume -y -mn -t $statuscmd -u $statussig && pkill -RTMIN+1 i3blocks

# cloud drive
exec --no-startup-id /home/stefan/.local/bin/mount_mmailbox_drive

